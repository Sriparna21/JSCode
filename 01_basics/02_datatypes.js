"use strict" // treat all the JS code as newer version

let name = "Srips" //string
let age = 18 // number - normal numbers
//bigint agar bahut hi bada number hai

let loggedIn = false // boolean - true/false
// null : standalone value
// undefined : no value defined
// null and undefined are different. Null strings are empty and undefined is where values are not defined
// symbol : defines components as unique

// object


console.log (typeof "??")

console.log (typeof undefined) // Result: undefined

console.log(typeof null) // Result: object